# base_blank · staging chat door

**Page (after R deploy with chat routes):**  
https://stg-r-production.up.railway.app/chat

**Mesh entry (works now):**  
`POST https://stg-r-production.up.railway.app/input`  
Header: `x-instance-key: <staging INSTANCE_KEY>`  
Body: `{"message":"..."}`

Full 7-position stack: `stg-r` … `stg-i` · entity `base_blank` · Neon staging branch.

U on cascade: R→U→B→I  
A on cascade: R→C→A→L→I
